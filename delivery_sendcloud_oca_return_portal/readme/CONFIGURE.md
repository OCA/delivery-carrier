Set *Return Operations From* in *Settings > Inventory > Sendcloud* to the date
you go live. Returns created in Sendcloud before it are synchronised as usual
but never turn into a warehouse operation, so an account with a history of
returns does not flood the warehouse on the first sync. Leave it empty to
process every return.

The *Create Return Operation* button on a return ignores the date.
