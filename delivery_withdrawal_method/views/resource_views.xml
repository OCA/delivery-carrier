<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_resource_calendar_attendance_tree_inherit" model="ir.ui.view">
        <field name="name">resource.calendar.attendance.tree.view_resource_calendar_attendance_tree_inherit</field>
        <field name="model">resource.calendar.attendance</field>
        <field name="priority" eval="20"/>
        <field name="inherit_id" ref="resource.view_resource_calendar_attendance_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree[1]" position="replace">
                <tree string="Working Time" editable="top">
                <field name="sequence" widget="handle"/>
                <field name="display_type" invisible="1"/>
                <field name="display_name" width="1" string=" " attrs="{'invisible': [('display_type', '!=', 'line_section')]}"/>
                <field name="name" attrs="{'invisible': [('display_type', '=', 'line_section')]}"/>
                <field name="dayofweek"/>
                <field name="day_period" optional="hide"/>
                <field name="hour_from" widget="float_time"/>
                <field name="hour_to" widget="float_time"/>
                <field name="hour_delay" optional="hide"/>
                <field name="select_type_delay" optional="hide"/>
                <field name="date_from" optional="hide"/>
                <field name="date_to" optional="hide"/>
                <field name="week_type" optional="hide" readonly="1" force_save="1" groups="base.group_no_one"/>
            </tree>
            </xpath>
        </field>
    </record>
</odoo>