On the carriers that ship several parcels per delivery (*Inventory >
Configuration > Delivery Methods*), tick *Tracking Reference per Package* to
refuse the validation of a delivery while any of its packages has no
reference. Packages must be enabled in the inventory settings. Fill in the *Tracking Link* of the carrier with
`<shipmenttrackingnumber>` as placeholder to get a tracking link per package.
